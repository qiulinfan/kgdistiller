interface:
  display_name: "Distill Paper"
  short_description: "Section guide, known concepts, and knowledge candidates"
  default_prompt: "Use $distill-paper to read this paper HTML-first, link existing knowledge and draft candidates for my selection before import."
policy:
  allow_implicit_invocation: false
